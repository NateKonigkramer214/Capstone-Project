@{
    ViewData["Title"] = "Home Page";
}
<div class="h-full">
    @*<div id="carouselExampleControls" class="carousel slide h-100" data-ride="carousel">
    <div class="carousel-inner">
    @foreach (var movie in ViewBag.movies)
    {
    <div class="carousel-item">
    <img class="d-block w-100" src="@movie.image" alt="@movie.title">
    </div>
    }
    </div>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
    </a>
    </div>

    <br />*@
    <h2 class="text-center text-2xl p-2 text-gray-200 shadow-2xl">Upcoming Movies</h2>

    <div class="grid grid-flow-row grid-cols-1 md:grid-cols-2 lg:grid-cols-3 flex justify-evenly auto-rows-max">
        @for (int i = 0; i < 6; i++)
        {
            var movie = ViewBag.movies[i];
            <div class="border-2 border-black m-2 p-2 rounded flex items-center justify-evenly bg-gray-400">
                <img class="h-[200px]" src="@movie.image" alt="@movie.title">
                <div class="text-center">
                    <h2 class="text-4xl font-bebas">@movie.title</h2>
                    <br/>
                    <p><strong>Genres:</strong> @movie.genres</p>
                    <div>
                        <p><strong>Release Date:</strong> @movie.releaseState</p>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
